node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment files and local secrets
.env
.env.*
!.env.example

# Runtime uploads (kept out of the image; created at build time)
uploads/*
!uploads/.gitkeep

# VCS and editor metadata
.git
.gitignore
.vscode

# OS metadata
.DS_Store

# Docker artifacts themselves
Dockerfile
.dockerignore
